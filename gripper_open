#!/bin/bash
echo "🤏 グリッパー開く..."
# トピック経由でグリッパー制御
ros2 topic pub --once /cobot/gripper_command std_msgs/msg/String "data: 'open'" > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✅ グリッパー開放コマンド送信"
else
    echo "❌ コマンド送信失敗"
fi
