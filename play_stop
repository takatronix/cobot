#!/bin/bash
echo "⏹️ モーション再生停止..."
# トピック経由で再生停止
ros2 topic pub --once /cobot/motion_stop std_msgs/msg/String "data: 'stop'" > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✅ 再生停止コマンド送信"
else
    echo "❌ コマンド送信失敗"
fi
